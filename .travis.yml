language: node_js
node_js:
  - 12
sudo: required
install:
  - npm install
script:
  - npm run build
deploy:
  provider: npm
  email: $EMAIL_ADDRESS
  api_key: $NPM_API
  skip_cleanup: true
  on:
    tags: true